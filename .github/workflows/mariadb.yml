name: CI - MariaDB Migration

on:
  push:
    branches:
    - main
  pull_request:
  # matches pull request for all branches and tag name
  
env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    AWS_REGION: ${{ secrets.AWS_REGION }}
    AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
    AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

defaults:
  run:
    shell: bash

jobs:
  start:
    name: start
    environment: start-stop
    concurrency: start-stop
    runs-on: ubuntu-latest
    needs: []
    if: (github.ref == 'refs/heads/main') && (github.event_name == 'push')
    steps:
    - name: Start deployment
      run: echo "Starting Deployment of RDS MariaDB."
        
  dev-deploy:
    name: dev-deploy
    environment: development
    concurrency: development
    runs-on: ubuntu-latest
    needs: [start]
    if: (github.ref == 'refs/heads/main') && (github.event_name != 'push')
    steps:
    - name: GitHub Actions Runner Set-up
      uses: actions/checkout@v2
    #- name: Terraform Repo Security Scan Set-up
    #  uses: triat/terraform-security-scan@v3
    - name: Terraform CLI Set-up
      uses: hashicorp/setup-terraform@v1
    - name: Terraform Init
      if: ${{ secret.INIT }} == true
      run: terraform init
    - name: Terraform Validate
      if: ${{ secret.VALIDATE }} == true
      run: terraform validate
    - name: Terraform Variables
      if: ${{ secret.PLAN }} == true || ${{ secret.APPLY }} == true
      run: |-
        cat dev.auto.tfvars <<EOF
        EOF
    - name: Terraform Plan
      if: ${{ secret.PLAN }} == true
      run: terraform plan
    - name: Terraform Apply
      if: ${{ secret.APPLY }} == true
      run: terraform apply -var-file=dev.auto.tfvars -auto-approve
    - name: Terraform Destroy
      if: ${{ secret.DESTROY }} == true
      run: terraform destroy -auto-approve
      #
